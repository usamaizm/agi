#ifndef TEXT_H
#define TEXT_H

#include "module.h"  // Include any necessary headers

// Define the TextModule structure
typedef struct {
    // Define text module-specific data structures or variables if needed
} TextModule;

// Declaration of the text module initialization function
Module init_text_module();

#endif  // TEXT_H
