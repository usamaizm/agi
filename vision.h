#ifndef VISION_H
#define VISION_H

#include "module.h"  // Include any necessary headers

// Define the VisionModule structure
typedef struct {
    // Define vision module-specific data structures or variables if needed
} VisionModule;

// Declaration of the vision module initialization function
Module init_vision_module();

#endif  // VISION_H
